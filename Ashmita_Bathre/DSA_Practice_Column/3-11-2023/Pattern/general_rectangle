CODE:
--------------

#include <iostream>
using namespace std;

int main() {
  int length, breadth;

  cout<<"Enter the lenght of the rect: ";
  cin>>length;
  cout<<endl<<"Enter the breadth of the rect: ";
  cin>>breadth;
  
  // outer loop for rows
  for(int row=0;row<breadth;row++)
  {
    // inner loop for column
    for(int col=0;col<length;col++)
    {
      cout<<"* ";
    }
    cout<<endl;
  }
  
  
}

OUTPUT:
-------------
Enter the lenght of the rect: 6

Enter the breadth of the rect: 4
* * * * * * 
* * * * * * 
* * * * * * 
* * * * * * 
