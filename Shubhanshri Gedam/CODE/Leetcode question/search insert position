class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
         int input;
          for(int i=0;i<nums.size();i++){
        if(nums[i]>=target){
            input=i;
            
            break;
        }
        
    }
    
    return input;
    }
};
