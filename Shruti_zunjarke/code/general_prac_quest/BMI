#include<iostream>
using namespace std;
int main(){
    float weight, height;
    cout<<"Weight(in kg): ";
    cin>>weight;
    cout<<"Height(in meters): ";
    cin>>height;
    float BMI = weight/(height*height);
    cout<<"BMI = "<<BMI<<endl;
    return 0;
}
